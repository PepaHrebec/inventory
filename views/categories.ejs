<!DOCTYPE html>
<html>
  <%- include('header'); -%>
  <body>
    <ul>
      <% categories.forEach(category => { %>
      <li><%= category.category_name %></li>
      <% }) %>
    </ul>
    <div>I work! Should be listing categories.</div>
  </body>
</html>
